import React from "react"

const tiber = (
  <span id="tiber"
    style={{
      height: 35 + "px",
      width: 35 + "px",
      backgroundColor: "var(--text-color)",
      textAlign: "center",
      color: "var(--primary-color)",
      fontFamily: "'Times New Roman', Times, serif",
      fontSize: 30 + "px"}}>
    T
  </span>
);

export default tiber